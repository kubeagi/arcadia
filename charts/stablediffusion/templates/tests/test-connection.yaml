apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "stablediffusion.fullname" . }}-test-connection"
  labels:
    {{- include "stablediffusion.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "stablediffusion.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
